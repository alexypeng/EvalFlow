services:
    postgres:
        image: postgres:16
        environment:
            POSTGRES_USER: postgres
            POSTGRES_PASSWORD: postgres
            POSTGRES_DB: evalflow
        ports:
            - "5432:5432"
        volumes:
            - evalflow_postgres:/var/lib/postgresql/data

volumes:
    evalflow_postgres:
